name: Android Build DDTO+
on: [pull_request, workflow_dispatch]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Haxe
        uses: krdlab/setup-haxe@v1
        with:
          haxe-version: 4.3.6

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r21e

      - name: Install Haxelibs
        run: |
          haxelib setup ~/haxelib
          haxelib install lime 7.9.0 --quiet
          haxelib install openfl 9.1.0 --quiet
          haxelib install flixel 4.11.0 --quiet
          haxelib install flixel-addons 2.11.0 --quiet
          haxelib install flixel-ui 2.4.0 --quiet
          haxelib install flixel-tools 1.5.1 --quiet
          haxelib install hscript --quiet
          haxelib install hxcpp 4.2.1 --quiet
          haxelib install firetongue --quiet
          haxelib git flxanimate https://github.com/Dot-Stuff/flxanimate tags/v1.2.0 --quiet
          haxelib git extension-webm https://github.com/KadeDev/extension-webm --quiet
          haxelib git linc_luajit https://github.com/nebulazorua/linc_luajit.git --quiet
          haxelib git extension-webview https://github.com/TeamX02/extension-webview.git --quiet

      - name: Patch Dependencies and Code
        run: |
          # Resolve erro de mouse na flxanimate (Android não possui FlxG.mouse)
          sed -i 's/FlxG.mouse/null/g' ~/haxelib/flxanimate/git/flxanimate/animate/FlxAnim.hx
          
          # Remove o símbolo $ que causa erro de Macro no Haxe 4.3+, 
          # mas garante que o final da linha tenha o ponto e vírgula.
          # Aplicado especificamente nas linhas problemáticas 99 e 102.
          sed -i '99s/\$//g; 99s/[[:space:]]*$/;/' source/KeyBindSubstate.hx
          sed -i '102s/\$//g; 102s/[[:space:]]*$/;/' source/KeyBindSubstate.hx
          
          # Limpeza final para evitar duplicatas de ;;
          sed -i 's/;;/;/g' source/KeyBindSubstate.hx

      - name: Configure Lime
        run: |
          haxelib run lime setup
          haxelib run lime config ANDROID_SDK $ANDROID_HOME
          haxelib run lime config ANDROID_NDK_ROOT $ANDROID_NDK_HOME
          haxelib run lime config JAVA_HOME $JAVA_HOME
          haxelib run lime config ANDROID_SETUP true

      - name: Compile
        run: haxelib run lime build android -64 -release -D lime-threads -D FLX_NO_MOUSE -w

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: DDTO-Plus-Android
          path: export/release/android/bin/app/build/outputs/apk/release/*.apk
